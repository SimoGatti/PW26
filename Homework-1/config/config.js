// =========================================================================
// CONFIGURAZIONE FRONTEND - API ENDPOINTS
// =========================================================================

const CONFIG = {
    // Se esegui il frontend in locale (es. con VS Code Live Server) e non hai PHP locale,
    // imposta useRemoteApi su true e inserisci il link completo del tuo api.php caricato su AlterVista.
    useRemoteApi: true, 
    remoteApiUrl: 'https://namenotfound.altervista.org/api.php' // Inserisci il link completo del tuo api.php su AlterVista
};
